{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SoftwareTitle list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Version</th>
                <th>Status</th>
                <th>Licensetype</th>
                <th>Remark</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('software_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.version }}</td>
                <td>{{ entity.status }}</td>
                <td>{{ entity.licenseType }}</td>
                <td>{{ entity.remark }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('software_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('software_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('software_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
