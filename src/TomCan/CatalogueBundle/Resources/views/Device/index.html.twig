{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Device list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Model</th>
                <th>Partno</th>
                <th>Serialno</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('devices_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.model }}</td>
                <td>{{ entity.partNo }}</td>
                <td>{{ entity.serialNo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('devices_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('devices_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('devices_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
